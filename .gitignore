# RStudio temporary files
.Rproj.user/

# R History files (can just re-run code instead)
.Rhistory
.Rapp.history

# Session Data files
.RData

# User-specific files
.Ruserdata

# Quarto files that don't need to be tracked.
scripts/.quarto/

# OAuth2 token, see https://github.com/hadley/httr/releases/tag/v0.3
.httr-oauth

# Temporary files created by R markdown
*.utf8.md
*.knit.md

# knitr and R markdown default cache directories
/*_cache/
/cache/

# R Environment Variables
.Renviron

# Omit data files to keep them off GitHub, but allow tracking the text files
# we use to automate creation of the `data` folder and its subfolders.
data/*.Rdata
data/*.Rds

# Omit HTML and PDF output plus log files created by rendering scripts.
*.pdf
scripts/*.html
scripts/*.log
scripts/*.pdf
scripts/output/*.html
scripts/output/*.log
scripts/output/*.pdf
